pragma solidity ^0.4.0;
contract EthereumDNS{
    // Define struct for reserving domain
    struct DNSRecord {
        address owner_addr;
        string ip_addr;
        uint end_timestamp;
    }
   
}